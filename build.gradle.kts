// Arquivo raiz de configuração do Gradle. Apenas declara os plugins que os
// módulos do projeto podem usar (aqui, só o módulo "app" os usa de fato).
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
