# Add project specific ProGuard rules here.
# Nesta etapa do projeto o minifyEnabled está desligado (isMinifyEnabled = false),
# então este arquivo não é usado, mas fica pronto para quando for habilitado.
